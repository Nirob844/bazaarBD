export enum tagTypes {
  user = "user",
  auth = "auth",
  admin = "admin",
  profile = "profile",
  category = "category",
  product = "product",
  cart = "cart",
  order = "order",
  promotion = "promotion",
}

export const tagTypesList = [
  tagTypes.user,
  tagTypes.auth,
  tagTypes.admin,
  tagTypes.profile,
  tagTypes.category,
  tagTypes.product,
  tagTypes.cart,
  tagTypes.order,
  tagTypes.promotion,
];
